# Optional root env for Docker / tunnel tooling.
# Application settings live in backend/.env (copy from backend/.env.example).

# Cloudflare Tunnel token (only if you run the cloudflared service)
CLOUDFLARE_TUNNEL_TOKEN=

# Public URL of your deployment (documentation / tooling only)
# SBC_PUBLIC_URL=https://example.com
